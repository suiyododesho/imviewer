# ✅ ページ遷移テスト - 実行ガイド

## 📊 テスト環境セットアップ

HTTPサーバーが起動しています。以下の手順でテストを実行してください。

### ステップ 1: テストツールにアクセス

ブラウザで以下のURLを開いてください：

```
http://localhost:8080/test.html
```

このページから自動テストとナビゲーション流れの検証ができます。

---

## 🧪 自動テスト

test.html ページの「すべてのテストを実行」ボタンをクリックすると、以下の項目が自動検証されます：

1. **生成済み構造データの読み込み** - `site/structure.json` を元に生成された `site/js/structure.js` の読み込み確認と、プロジェクト・人物・ギャラリーの総数表示
2. **HTMLファイルの存在確認** - index.html, project.html, person.html, gallery.html
3. **JavaScriptファイルの存在確認** - navigation.js, search.js, structure.js, gallery-pages.js, 各ページのjs
4. **画像ファイルの存在確認** - banner/, thumbnail/ フォルダ
5. **ナビゲーションフロー例示** - サンプルURL表示

運用メモ:

```powershell
cd d:\Tool\_mytool\_bulk\sitedesign
.\.venv\Scripts\python.exe tools\build_gallery_pages_map.py
```

`site/structure.json` を修正した後は、上記コマンドで `site/js/structure.js` と `site/js/gallery-pages.js` を再生成してからテストしてください。

---

## 🔄 手動テスト - ページ遷移フロー

### フロー 1: トップ → 企画 → 人物 → ギャラリー

1. **[index.html](http://localhost:8080/index.html)** を開く
   - ✓ ヘッダーに「Photo Gallery」と検索ボックスが表示されている
   - ✓ 企画バナーが2列グリッドで表示されている
   - ✓ 各企画の下に人物名が表示されている

2. **企画バナーをクリック** → project.html へ遷移
   - ✓ パンくずリスト「トップ > 企画名」が表示されている
   - ✓ 企画バナーが大きく表示されている
   - ✓ 企画内のすべての人物が見出しとして表示されている
   - ✓ 各人物のギャラリーが4列グリッドで表示されている

3. **人物見出しをクリック** → person.html へ遷移
   - ✓ パンくずリスト「トップ > 企画名 > 人物名」が表示されている
   - ✓ その人物のすべてのギャラリーが表示されている
   - ✓ 下部に「前へ」「次へ」ボタンがある
   - ✓ ナビゲーション情報「n / total」が表示されている

4. **ギャラリーサムネイルをクリック** → gallery.html へ遷移
   - ✓ パンくずリスト「トップ > 企画名 > 人物名 > ギャラリー名」が表示されている
   - ✓ メイン領域にギャラリー画像が直接表示されている
   - ✓ 左側に縦並びのサムネイル一覧が表示されている
   - ✓ 下部に「前へ」「次へ」ボタン、位置情報、キーボード操作説明がある
   - ✓ 「前へ」「次へ」で画像ページが切り替わる

### フロー 2: パンくずリストでの戻り遷移

各ページでパンくずリストをテスト：
- person.html から「企画名」をクリック → project.html に戻る ✓
- project.html から「トップ」をクリック → index.html に戻る ✓
- gallery.html から「人物名」をクリック → person.html に戻る ✓

### フロー 3: 前後ボタンテスト

- person.html で「前へ」「次へ」をクリック → 同じ企画内の隣人物に移動 ✓
- gallery.html で「前へ」「次へ」をクリック → 同じ人物内の隣ギャラリーに移動 ✓
- 最初/最後の項目でボタンが無効化される ✓

### フロー 4: 検索機能テスト

- index.html の検索ボックスに人物名を入力 → 該当人物のギャラリーが表示される ✓
- 存在しない人物名を入力 → 「該当なし」が表示される ✓

---

## 📋 テスト結果リスト

### ✅ 期待される成功シナリオ

```
✓ すべてのHTMLファイルが存在
✓ structure.json から生成された structure.js / gallery-pages.js が正常に読み込める
✓ 4つのページすべてにアクセス可能
✓ ページ間のナビゲーションが正常に動作
✓ 検索機能が動作
✓ 画像ビューアでギャラリー画像が表示される
```

### ❌ 問題が発生した場合

| 問題 | 原因・解決策 |
|------|-----------|
| `Cannot GET /` | ファイルのパスが正しいか確認。必ず `http://localhost:8080/index.html` のようにファイルを指定 |
| ギャラリーが表示されない | ブラウザのコンソール (F12) でエラー確認。`site/structure.json` 修正後に `tools/build_gallery_pages_map.py` を実行したか確認 |
| 画像が表示されない | `banner/` `thumbnail/` ディレクトリが site/ にあるか確認 |
| なぜか file:// でうまくいかない | HTTPサーバーを使用。`python -m http.server 8080` で起動 |
| スタイルが適用されない | CSS ファイルが読み込まれているか確認。キャッシュをクリア (Ctrl+Shift+Delete) |

---

## 🛠️ サーバー管理

### サーバーの状態確認

HTTPサーバーは以下のコマンドで起動しています：

```bash
cd site/
python -m http.server 8080 --bind 127.0.0.1
```

構造データを更新した場合は、サーバー起動前またはリロード前に以下も実行してください：

```powershell
cd d:\Tool\_mytool\_bulk\sitedesign
.\.venv\Scripts\python.exe tools\build_gallery_pages_map.py
```

### サーバーの停止

ターミナルで `Ctrl+C` を押すと停止します。

### サーバーの再起動

ターミナルで同じコマンドを再実行してください。

---

## 📊 テスト用URL一覧

| ページ | URL | 説明 |
|-------|-----|-----|
| テストツール | http://localhost:8080/test.html | 自動テスト実行 |
| 01. トップ | http://localhost:8080/index.html | 企画一覧 |
| 02. 企画ページ | http://localhost:8080/project.html?project=honey2 | サンプル企画 |
| 03. 人物ページ | http://localhost:8080/person.html?project=honey2&person=薫まい | サンプル人物 |
| 04. ギャラリー | http://localhost:8080/gallery.html?project=honey2&person=薫まい&gallery=photo/honey2/薫まい%2001/index_薫まい%2001.html | サンプルギャラリー |

※ サンプル URL は `site/structure.json` の現在値に合わせて読み替えてください。企画名やパスを変更した場合は、再生成後の値を使います。

---

## ✨ テスト完了の確認事項

以下すべてにチェックがつけば、ページ遷移は正常に実装されています：

- [ ] トップページが正常に表示される
- [ ] 企画バナーをクリックで企画ページに移動
- [ ] 人物名をクリックで人物ページに移動
- [ ] ギャラリーをクリックでギャラリーページに移動
- [ ] すべてのパンくずリンクが正常に動作
- [ ] 前後ボタンが正常に動作
- [ ] 検索機能が正常に動作
- [ ] エラー処理が正常に動作

---

## 📞 トラブルシューティング連絡先

テスト中に問題が発生した場合、以下の情報を記録してください：

- ブラウザ種類とバージョン
- エラーメッセージ（コンソール出力）
- 発生したページと操作
- 期待される動作と実際の動作

